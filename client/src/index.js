import React from 'react';
import ReactDOM from 'react-dom/client';      // or 'react-dom' in older CRA
import App from './App';

const root = ReactDOM.createRoot(
  document.getElementById('root')
);
root.render(<App />);
